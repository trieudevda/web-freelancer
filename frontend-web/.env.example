# URL công khai của NestJS, gồm cả API_PREFIX. Bỏ trống để chạy giao diện demo.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Giới hạn kiểm tra sớm ở trình duyệt; backend vẫn là nơi quyết định cuối cùng.
NEXT_PUBLIC_MEDIA_MAX_IMAGE_MB=10
NEXT_PUBLIC_MEDIA_MAX_VIDEO_MB=1024
